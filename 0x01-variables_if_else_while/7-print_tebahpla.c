#include <stdio.h>

/**
 * main - Prints the lowercase alphabet in reverse.
 * 
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ch;
	
	for (ch = 'z'; ch >= 'a'; i++)
	{
		putChar(ch)
		
	}
	putChar('\n');
	return (0);
} 	
