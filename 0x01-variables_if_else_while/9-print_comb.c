#include <stdio.h>

/**
 * main - Prints all possible combination of single-digit numbers.
 * 
 * Return: Always 0 (Success)
 */
int main(void)
{
	int n;
	
	for (n = 48; n < 58; n++)
	{
		putChar(n)
		if (n != 57)
		{
			putChar(',');
			putChar('');
		}
		
	}
	putChar('\n');
	return (0);
}
